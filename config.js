window.AIM_CONFIG={supabaseUrl:"https://edadwxggqqjugldngfll.supabase.co",supabaseAnonKey:"sb_publishable_mOwditWAOlsz5Xq32b2g8g_6goNvfz0"};

/* AIM TRAINER — COMBO UPDATE 1.1 */
(()=>{
'use strict';
const PROFILE='aimTrainerProfileV3',SOUND='aimTrainerSoundV1';let streak=0,maxStreak=0,sound=localStorage.getItem(SOUND)!=='off',ctx=null;
const el=id=>document.getElementById(id),read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))||f}catch{return f}};
function beep(f=650,d=.06){if(!sound)return;const A=window.AudioContext||window.webkitAudioContext;if(!A)return;ctx=ctx||new A();const o=ctx.createOscillator(),g=ctx.createGain();o.type='square';o.frequency.value=f;g.gain.setValueAtTime(.025,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+d);o.connect(g);g.connect(ctx.destination);o.start();o.stop(ctx.currentTime+d)}
function profile(){return{best:0,rounds:0,combo:0,...read(PROFILE,{})}}
function drawProfile(){const p=profile();if(el('pBest'))el('pBest').textContent=p.best;if(el('pRounds'))el('pRounds').textContent=p.rounds;if(el('pCombo'))el('pCombo').textContent=p.combo+'×'}
function rank(n){return n>=50?'👑 Мастер':n>=40?'💎 Алмаз':n>=30?'🥇 Золото':n>=20?'🥈 Серебро':'🥉 Бронза'}
function install(){
 const style=document.createElement('style');style.textContent=`.stats{grid-template-columns:repeat(6,1fr)!important}.comboFx{position:absolute;z-index:9;top:22px;left:50%;transform:translateX(-50%);font-size:34px;font-weight:1000;text-shadow:0 0 24px #ff3b5c;animation:cf .7s forwards}@keyframes cf{0%{opacity:0;transform:translateX(-50%) scale(.5)}35%{opacity:1;transform:translateX(-50%) scale(1.1)}100%{opacity:0}}.profileMini{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-bottom:12px}.profileMini div{padding:9px 5px;text-align:center;border:1px solid var(--line);border-radius:11px;background:#ffffff08}.profileMini span{display:block;color:var(--muted);font-size:8px;text-transform:uppercase}.profileMini strong{display:block;margin-top:3px}.rankTag{display:block;margin-top:2px;color:var(--muted);font-size:8px;text-transform:uppercase}.soundBtn{margin-left:8px;border:1px solid var(--line);border-radius:11px;background:#ffffff0b;color:white;padding:9px;cursor:pointer}.rankBanner{margin:12px 0 0;padding:10px;border:1px solid #ffd16655;border-radius:12px;color:#ffe39a;background:#ffd16612;font-weight:900}@media(max-width:900px){.stats{grid-template-columns:repeat(3,1fr)!important}}`;document.head.appendChild(style);
 const stats=document.querySelector('.stats');if(stats&&!el('comboValue')){const d=document.createElement('div');d.className='stat';d.innerHTML='<span>Серия</span><strong id="comboValue">0×</strong>';stats.appendChild(d)}
 const logo=document.querySelector('.logo');if(logo){logo.insertAdjacentHTML('beforeend','<button class="soundBtn" id="soundBtn" title="Звук">'+(sound?'🔊':'🔇')+'</button>')}
 const status=el('status');if(status){const p=document.createElement('div');p.className='profileMini';p.innerHTML='<div><span>Рекорд</span><strong id="pBest">0</strong></div><div><span>Раундов</span><strong id="pRounds">0</strong></div><div><span>Серия</span><strong id="pCombo">0×</strong></div>';status.insertAdjacentElement('afterend',p)}
 el('soundBtn')?.addEventListener('click',()=>{sound=!sound;localStorage.setItem(SOUND,sound?'on':'off');el('soundBtn').textContent=sound?'🔊':'🔇';if(sound)beep(800)})
 drawProfile();
}
function comboFx(){if(streak<5||streak%5)return;const d=document.createElement('div');d.className='comboFx';d.textContent=streak+'× COMBO';el('arena').appendChild(d);setTimeout(()=>d.remove(),750)}
function dedupe(list){const m=new Map();for(const x of list||[]){const k=String(x.name||'').trim().toLowerCase(),old=m.get(k);if(!old||x.score>old.score||x.score===old.score&&x.reaction<old.reaction)m.set(k,x)}return[...m.values()].sort((a,b)=>b.score-a.score||a.reaction-b.reaction)}
function hooks(){
 el('target')?.addEventListener('pointerdown',()=>{if(typeof running==='undefined'||!running)return;streak++;maxStreak=Math.max(maxStreak,streak);if(el('comboValue'))el('comboValue').textContent=streak+'×';beep(streak%5===0?900:680);comboFx()},true);
 el('arena')?.addEventListener('pointerdown',e=>{if(e.target===el('target')||typeof running==='undefined'||!running)return;streak=0;if(el('comboValue'))el('comboValue').textContent='0×';beep(150,.09)},true);
 el('start')?.addEventListener('click',()=>{streak=0;maxStreak=0;if(el('comboValue'))el('comboValue').textContent='0×'},true);
 if(typeof render==='function')render=(orig=>function(list){orig(dedupe(list));document.querySelectorAll('.score').forEach((row,i)=>{if(i===0)row.style.borderColor='#ffd16655';const score=Number(row.querySelector('.points')?.textContent||0),s=document.createElement('span');s.className='rankTag';s.textContent=rank(score);row.querySelector('.player')?.appendChild(s)})})(render);
 if(typeof finish==='function')finish=(orig=>async function(){const before=profile();await orig();const score=Number(el('rHits')?.textContent||0),p=profile();p.best=Math.max(p.best,score);p.rounds++;p.combo=Math.max(p.combo,maxStreak);localStorage.setItem(PROFILE,JSON.stringify(p));drawProfile();const box=el('result');if(box&&!el('rankBanner')){const b=document.createElement('div');b.id='rankBanner';b.className='rankBanner';box.insertAdjacentElement('afterend',b)}if(el('rankBanner'))el('rankBanner').textContent=rank(score)+(score>before.best?' · Новый рекорд!':'')})(finish)
}
function init(){install();hooks();setTimeout(()=>typeof loadScores==='function'&&loadScores(),0)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();

/* AIM TRAINER — PROGRESSION UPDATE 1.2 */
(()=>{
'use strict';
const PROGRESS_KEY='aimTrainerProgressV1',DAILY_KEY='aimTrainerDailyV1',HISTORY_KEY='aimTrainerHistoryV1';
const $=id=>document.getElementById(id),read=(key,fallback)=>{try{return JSON.parse(localStorage.getItem(key))||fallback}catch{return fallback}},write=(key,value)=>localStorage.setItem(key,JSON.stringify(value));
let roundCombo=0,roundMaxCombo=0;
const today=()=>new Date().toISOString().slice(0,10);
function progress(){return{xp:0,achievements:[],...read(PROGRESS_KEY,{})}}
function levelData(xp){let level=1,left=xp,need=300;while(left>=need){left-=need;level++;need=300+(level-1)*100}return{level,current:left,need,percent:Math.min(100,Math.round(left/need*100))}}
function missions(){const day=Number(today().replaceAll('-','').slice(-4)),score=25+(day%3)*5,accuracy=85+(day%3)*5,combo=8+(day%3)*2;return[
 {id:'score',title:'Охотник',text:'Попади по '+score+' целям за раунд',target:score,reward:120},
 {id:'accuracy',title:'Снайпер',text:'Заверши раунд с точностью '+accuracy+'%',target:accuracy,reward:100},
 {id:'combo',title:'Без промаха',text:'Собери серию '+combo+'×',target:combo,reward:140}
]}
function daily(){const saved=read(DAILY_KEY,{});if(saved.date!==today())return{date:today(),done:{}};return{date:today(),done:saved.done||{}}}
function saveDaily(value){write(DAILY_KEY,value)}
function history(){return read(HISTORY_KEY,[])}
function achievementList(){return[
 {id:'first',icon:'🎯',title:'Первый шаг',text:'Завершить первый раунд'},
 {id:'score30',icon:'⚡',title:'Тридцатка',text:'Попасть по 30 целям за раунд'},
 {id:'accurate',icon:'🔭',title:'Идеальная рука',text:'Получить точность не ниже 95%'},
 {id:'combo10',icon:'🔥',title:'На серии',text:'Собрать серию 10×'},
 {id:'veteran',icon:'🏅',title:'Ветеран',text:'Завершить 25 раундов'}
]}
function installStyles(){const style=document.createElement('style');style.textContent=`
.progressCard{margin-bottom:12px;padding:12px;border:1px solid var(--line);border-radius:14px;background:linear-gradient(135deg,#8b5cf61a,#22d3ee0c)}.progressTop{display:flex;align-items:center;justify-content:space-between;gap:8px}.levelBadge{font-weight:950}.xpText{color:var(--muted);font-size:10px}.xpBar{height:7px;margin:9px 0 10px;border-radius:999px;overflow:hidden;background:#ffffff0b}.xpFill{height:100%;width:0;border-radius:inherit;background:linear-gradient(90deg,#8b5cf6,#22d3ee);box-shadow:0 0 16px #8b5cf666;transition:width .5s ease}.progressButton{width:100%;border:1px solid #8b5cf655;border-radius:10px;padding:9px;color:#fff;background:#8b5cf618;cursor:pointer;font-weight:850}.progressButton:hover{background:#8b5cf62b}.progressDot{display:inline-grid;min-width:20px;height:20px;margin-left:6px;place-items:center;border-radius:999px;background:#ff3b5c;color:white;font-size:10px}.progressModal{position:fixed;z-index:1000;inset:0;display:none;place-items:center;padding:18px;background:#05060bd9;backdrop-filter:blur(12px)}.progressModal.show{display:grid}.progressWindow{width:min(720px,100%);max-height:min(760px,90dvh);overflow:auto;border:1px solid var(--line);border-radius:24px;padding:22px;background:#11131df7;box-shadow:0 30px 100px #000b}.progressHeader{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:18px}.progressHeader h2{margin:0;font-size:25px}.progressClose{width:40px;height:40px;border:1px solid var(--line);border-radius:12px;color:white;background:#ffffff0a;cursor:pointer}.progressTabs{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-bottom:16px}.progressTab{border:1px solid var(--line);border-radius:10px;padding:10px;color:var(--muted);background:#ffffff07;cursor:pointer;font-weight:850}.progressTab.active{color:white;border-color:#8b5cf666;background:#8b5cf61f}.progressPane{display:none}.progressPane.active{display:block}.missionList,.achievementList,.historyList{display:grid;gap:9px}.mission,.achievement,.historyRow{padding:13px;border:1px solid var(--line);border-radius:13px;background:#ffffff07}.mission{display:grid;grid-template-columns:1fr auto;align-items:center;gap:12px}.mission.done{border-color:#42f5a455;background:#42f5a40b}.mission strong,.achievement strong{display:block}.mission small,.achievement small,.historyRow small{display:block;margin-top:4px;color:var(--muted)}.missionReward{font-weight:950;color:#ffd166}.achievement{display:grid;grid-template-columns:38px 1fr;align-items:center;gap:10px;opacity:.42}.achievement.unlocked{opacity:1;border-color:#ffd16655}.achievementIcon{font-size:25px}.historyRow{display:grid;grid-template-columns:1fr auto;align-items:center;gap:12px}.historyStats{text-align:right;font-weight:900}.progressToast{position:fixed;z-index:1100;right:18px;bottom:18px;max-width:330px;padding:14px 17px;border:1px solid #8b5cf666;border-radius:14px;background:#161928f5;box-shadow:0 18px 55px #000a;animation:toastIn .35s ease,toastOut .35s ease 3.1s forwards}.progressToast strong,.progressToast span{display:block}.progressToast span{margin-top:4px;color:var(--muted);font-size:12px}@keyframes toastIn{from{opacity:0;transform:translateY(18px)}}@keyframes toastOut{to{opacity:0;transform:translateY(18px)}}.versionTag{margin-left:8px;padding:3px 6px;border:1px solid #8b5cf655;border-radius:7px;color:#bfa8ff;font-size:8px;letter-spacing:.08em}@media(max-width:520px){.progressWindow{padding:17px}.progressTabs{grid-template-columns:1fr}.progressToast{left:10px;right:10px;bottom:10px}}
`;document.head.appendChild(style)}
function installUi(){
 const logo=document.querySelector('.logo');if(logo&&!$('versionTag'))logo.insertAdjacentHTML('beforeend','<span class="versionTag" id="versionTag">v1.2</span>');
 const mini=document.querySelector('.profileMini');if(mini&&!$('progressCard'))mini.insertAdjacentHTML('afterend','<div class="progressCard" id="progressCard"><div class="progressTop"><strong class="levelBadge" id="levelBadge">Уровень 1</strong><span class="xpText" id="xpText">0 / 300 XP</span></div><div class="xpBar"><div class="xpFill" id="xpFill"></div></div><button class="progressButton" id="progressOpen">Прогресс и задания <span class="progressDot" id="progressDot">3</span></button></div>');
 if(!$('progressModal'))document.body.insertAdjacentHTML('beforeend','<div class="progressModal" id="progressModal"><div class="progressWindow"><div class="progressHeader"><div><div class="eyebrow">Progression Update 1.2</div><h2>Профиль игрока</h2></div><button class="progressClose" id="progressClose">✕</button></div><div class="progressTabs"><button class="progressTab active" data-progress="missions">Задания</button><button class="progressTab" data-progress="achievements">Достижения</button><button class="progressTab" data-progress="history">История</button></div><section class="progressPane active" id="pane-missions"><div class="missionList" id="missionList"></div></section><section class="progressPane" id="pane-achievements"><div class="achievementList" id="achievementList"></div></section><section class="progressPane" id="pane-history"><div class="historyList" id="historyList"></div></section></div></div>');
 $('progressOpen')?.addEventListener('click',()=>{$('progressModal').classList.add('show');renderAll()});$('progressClose')?.addEventListener('click',()=>$('progressModal').classList.remove('show'));$('progressModal')?.addEventListener('pointerdown',event=>{if(event.target===$('progressModal'))$('progressModal').classList.remove('show')});
 document.querySelectorAll('.progressTab').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.progressTab').forEach(x=>x.classList.toggle('active',x===button));document.querySelectorAll('.progressPane').forEach(x=>x.classList.toggle('active',x.id==='pane-'+button.dataset.progress))}));
}
function toast(title,text){const old=document.querySelector('.progressToast');if(old)old.remove();const node=document.createElement('div');node.className='progressToast';node.innerHTML='<strong></strong><span></span>';node.querySelector('strong').textContent=title;node.querySelector('span').textContent=text;document.body.appendChild(node);setTimeout(()=>node.remove(),3600)}
function renderAll(){const p=progress(),l=levelData(p.xp),d=daily();if($('levelBadge'))$('levelBadge').textContent='Уровень '+l.level;if($('xpText'))$('xpText').textContent=l.current+' / '+l.need+' XP';if($('xpFill'))$('xpFill').style.width=l.percent+'%';
 const left=missions().filter(m=>!d.done[m.id]).length;if($('progressDot')){$('progressDot').textContent=left;$('progressDot').style.display=left?'inline-grid':'none'}
 if($('missionList'))$('missionList').innerHTML=missions().map(m=>'<div class="mission '+(d.done[m.id]?'done':'')+'"><div><strong>'+(d.done[m.id]?'✓ ':'')+m.title+'</strong><small>'+m.text+'</small></div><div class="missionReward">+'+m.reward+' XP</div></div>').join('');
 const unlocked=new Set(p.achievements||[]);if($('achievementList'))$('achievementList').innerHTML=achievementList().map(a=>'<div class="achievement '+(unlocked.has(a.id)?'unlocked':'')+'"><div class="achievementIcon">'+a.icon+'</div><div><strong>'+a.title+'</strong><small>'+a.text+(unlocked.has(a.id)?' · Получено':'')+'</small></div></div>').join('');
 const items=history();if($('historyList'))$('historyList').innerHTML=items.length?items.map(item=>'<div class="historyRow"><div><strong>'+new Date(item.date).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})+'</strong><small>Серия '+item.combo+'× · реакция '+(item.reaction||'—')+' мс</small></div><div class="historyStats">'+item.score+' целей<br><small>'+item.accuracy+'%</small></div></div>').join(''):'<div class="empty">Сыграй первый раунд, чтобы появилась история</div>'}
function unlock(ids,p){let gained=0;const set=new Set(p.achievements||[]),newOnes=[];for(const id of ids){if(!set.has(id)){set.add(id);gained+=75;newOnes.push(achievementList().find(a=>a.id===id))}}p.achievements=[...set];return{gained,newOnes}}
function processRound(){const score=Number($('rHits')?.textContent||0),accuracy=Number(String($('rAccuracy')?.textContent||'0').replace(/\D/g,'')),reaction=Number(String($('rReaction')?.textContent||'0').replace(/\D/g,''))||0,rounds=Number($('pRounds')?.textContent||0),p=progress(),before=levelData(p.xp).level,d=daily();let xp=score*3+Math.floor(accuracy/10)+Math.floor(roundMaxCombo/2),missionXp=0;
 for(const m of missions()){let complete=false;if(m.id==='score')complete=score>=m.target;if(m.id==='accuracy')complete=accuracy>=m.target;if(m.id==='combo')complete=roundMaxCombo>=m.target;if(complete&&!d.done[m.id]){d.done[m.id]=true;missionXp+=m.reward}}saveDaily(d);
 const candidates=[];if(rounds>=1)candidates.push('first');if(score>=30)candidates.push('score30');if(accuracy>=95)candidates.push('accurate');if(roundMaxCombo>=10)candidates.push('combo10');if(rounds>=25)candidates.push('veteran');const unlocked=unlock(candidates,p);xp+=missionXp+unlocked.gained;p.xp+=xp;write(PROGRESS_KEY,p);
 const list=history();list.unshift({date:new Date().toISOString(),score,accuracy,reaction,combo:roundMaxCombo});write(HISTORY_KEY,list.slice(0,12));renderAll();const after=levelData(p.xp).level;if(after>before)toast('Новый уровень: '+after,'Получено '+xp+' XP за раунд');else if(unlocked.newOnes.length)toast('Достижение: '+unlocked.newOnes[0].title,'Получено '+xp+' XP за раунд');else if(missionXp)toast('Задание выполнено','Получено '+xp+' XP за раунд');else toast('+'+xp+' XP','Уровень '+after+' · серия '+roundMaxCombo+'×')}
function hooks(){
 $('target')?.addEventListener('pointerdown',()=>{if(typeof running==='undefined'||!running)return;roundCombo++;roundMaxCombo=Math.max(roundMaxCombo,roundCombo)},true);
 $('arena')?.addEventListener('pointerdown',event=>{if(event.target===$('target')||typeof running==='undefined'||!running)return;roundCombo=0},true);
 $('start')?.addEventListener('click',()=>{roundCombo=0;roundMaxCombo=0},true);
 if(typeof finish==='function')finish=(original=>async function(){await original();processRound()})(finish)
}
function init(){installStyles();installUi();hooks();renderAll()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
